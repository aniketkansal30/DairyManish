{
  "name": "manish-dairy",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm install --prefix frontend clear&& npm run build --prefix frontend",
    "start": "node backend/server.js",
    "dev": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && node backend/server.js"
  }
}